<%= form_for(subitem_category) do |f| %>
  <% if subitem_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subitem_category.errors.count, "error") %> prohibited this subitem_category from being saved:</h2>

      <ul>
      <% subitem_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_area :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
