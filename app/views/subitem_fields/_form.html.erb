<%= form_for(subitem_field) do |f| %>
  <% if subitem_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subitem_field.errors.count, "error") %> prohibited this subitem_field from being saved:</h2>

      <ul>
      <% subitem_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_area :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
